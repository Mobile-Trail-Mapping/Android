<?xml version="1.0" encoding="utf-8"?>
<menu
	xmlns:android="http://schemas.android.com/apk/res/android">
	<group
		android:id="@+id/admin">
		<item
			android:id="@+id/menu_edit_point"
			android:icon="@drawable/ic_menu_edit"
			android:title="@string/edit_this_point"
			android:titleCondensed="@string/edit"
			android:enabled="false"/>
		<item
			android:id="@+id/menu_add_photo"
			android:icon="@drawable/ic_menu_camera"
			android:title="@string/add_another_photo"
			android:titleCondensed="@string/add_photo"/>
	</group>
</menu>